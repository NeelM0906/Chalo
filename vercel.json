{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "installCommand": "npm i -g npm@9 && npm ci",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "frontend/dist/index.html" }
  ],
  "env": {
    "NODE_VERSION": "20",
    "ROLLUP_SKIP_NODEJS_NATIVE": "true"
  }
}

